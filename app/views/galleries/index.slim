
/ - if @galleries
/ 	- @galleries.each do |gallery|
/ 		p 
/ 			span.uk-text-bold Gallery Name: 
/ 			= gallery.name
/ 		p 
/ 			span.uk-text-bold Number of Photos: 
/ 			= gallery.photos.size 
/ 		- if gallery.photos.size > 0
/ 			div.uk-grid
/ 				- gallery.photos.each do |photo|
/ 					div.uk-width-medium-1-3.uk-width-small-1-2
/ 						img src= photo.url
/ 						p.uk-text-small= photo.title
/ - else 
/ 	p NO GALLERIES			

- if @galleries
	div.uk-grid
		- @galleries.each do |gallery|
			div.uk-width-medium-1-3
				=link_to image_tag(gallery.first_photo), gallery_path(gallery.id)
				p=link_to gallery.name, gallery_path(gallery.id)
- else 
	p NO GALLERIES					

