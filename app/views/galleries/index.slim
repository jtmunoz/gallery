
/ - if @galleries
/ 	- @galleries.each do |gallery|
/ 		p 
/ 			span.uk-text-bold Gallery Name: 
/ 			= gallery.name
/ 		p 
/ 			span.uk-text-bold Number of Photos: 
/ 			= gallery.photos.size 
/ 		- if gallery.photos.size > 0
/ 			div.uk-grid
/ 				- gallery.photos.each do |photo|
/ 					div.uk-width-medium-1-3.uk-width-small-1-2
/ 						img src= photo.url
/ 						p.uk-text-small= photo.title
/ - else 
/ 	p NO GALLERIES			

- if @galleries
	- @galleries.each do |gallery|
		=link_to image_tag(gallery.photos.first.url), gallery_path(gallery.id)
			
